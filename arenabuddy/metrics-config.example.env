# Arenabuddy Metrics Configuration
# Copy this file to .env and adjust the values as needed

# Enable or disable metrics collection
# Default: true
ARENABUDDY_METRICS_ENABLED=true

# URL to push metrics to
# This can be a Prometheus Push Gateway or any compatible endpoint
# Examples:
#   - Prometheus Push Gateway: http://localhost:9091/metrics/job/arenabuddy
#   - Grafana Cloud: https://prometheus-prod-us-central1.grafana.net/api/prom/push
#   - Custom endpoint: https://your-metrics-server.com/api/v1/metrics
# Leave unset to disable remote push (metrics will still be collected locally)
ARENABUDDY_METRICS_URL=http://localhost:9091/metrics/job/arenabuddy

# How often to push metrics (in seconds)
# Default: 300 (5 minutes)
ARENABUDDY_METRICS_INTERVAL=300

# Unique instance identifier
# This helps distinguish between multiple Arenabuddy instances
# Default: auto-generated UUID
# ARENABUDDY_INSTANCE_ID=my-desktop-instance

# Optional: Standard database configuration
# ARENABUDDY_DATABASE_URL=postgresql://user:password@localhost/arenabuddy

# Example configurations for different setups:

# === Local Prometheus Push Gateway ===
# ARENABUDDY_METRICS_URL=http://localhost:9091/metrics/job/arenabuddy/instance/desktop

# === Grafana Cloud (requires authentication) ===
# Note: For authenticated endpoints, you may need to modify the code to add auth headers
# ARENABUDDY_METRICS_URL=https://prometheus-prod-us-central1.grafana.net/api/prom/push
# GRAFANA_USER=your-user-id
# GRAFANA_API_KEY=your-api-key

# === Docker Compose Setup ===
# ARENABUDDY_METRICS_URL=http://pushgateway:9091/metrics/job/arenabuddy

# === Disable metrics completely ===
# ARENABUDDY_METRICS_ENABLED=false