load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "tracingx",
    srcs = glob(["src/**/*.rs"]),
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:tracing",
        "@crates//:tracing-subscriber",
    ],
)