load("@rules_rust//rust:defs.bzl", "rust_analyzer")

rust_analyzer(
    name = "rust_analyzer",
    targets = [
        "//...:all",
        # Add all your rust targets here
        # Or use a glob pattern like "//...:all"
    ],
)
